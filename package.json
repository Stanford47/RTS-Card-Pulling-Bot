{
    "name": "rts-pack-bot",
    "description": "bot for pulling cards in rts discord server",
    "engines": {
        "node": "^21.5.0"
    },
    "dependencies": {
        "typescript": "^5.3.3",
        "discord.js": "^14.14.1",
        "@discordjs/builders": "^1.7.0",
        "dotenv": "^16.3.1"
    },
    "devDependencies": {},
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc --build",
        "build-clean": "sudo rm -rf dist && tsc --build",
        "clean": "sudo rm -rf dist",
        "run": "node dist/index.js"
    }
}